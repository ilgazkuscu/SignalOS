# Deployment Readiness

## Requirements before public deployment
- Add authentication
- Decide fixture vs live mode defaults
- Configure env vars
- Validate live source terms/access
- Add persistent storage for snapshots and decisions
- Add monitoring/logging

## Build
`npm run build` passes, with expected dynamic-route logs for live fetches.
